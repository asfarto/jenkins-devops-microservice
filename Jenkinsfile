node {
	stage('Build') {
		echo "Build"
	}
	stage('Test') {
		echo "Test"
	}
	stage('Integration Test') {
		echo "Test"
	}
}

//stage lines are optionals, but useful when we have a lot of stage to execute.
//It could be so:
//node {
//		echo "Build"
//		echo "Test"
//		echo "Test"
//}
